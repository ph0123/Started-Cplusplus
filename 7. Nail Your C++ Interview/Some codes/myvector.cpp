#include <iostream>
#include <cmath>
#include <vector>
using namespace std;

int main()
{
    vector<int> arr;
    arr.push_back(5);
    arr.push_back(1);
    arr.push_back(9);
    for (int i = 0; i< arr.size(); i++)
    {
        cout<< arr[i]<<endl;
    }
    
}
